// Config for the Lulla push backend (Neon Function). The URL is set once at
// deploy time; credentials live per-device in localStorage, see pushCred.ts.
export const PUSH_API = 'https://br-bitter-frost-b4p0l50q-push.compute.c-6.us-east-2.aws.neon.tech'